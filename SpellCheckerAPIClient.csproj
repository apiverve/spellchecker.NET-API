<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.SpellChecker</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/spellchecker</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Spell Checker is a simple tool for checking spelling. It returns the corrected text.</Description>
    <RepositoryUrl>https://github.com/apiverve/spellchecker-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>spell checker; spell check; spell checker api; spell check api; spell checker tool; spell check tool; spell checker software; spell check software; spell checker service; spell check service; spell checker library; spell check library; spell checker sdk; spell check sdk; spell checker code; spell check code; spell checker script; spell check script; spell checker program; spell check program; spell checker application; spell check application; spell checker app; spell check app; spell checker utility; spell check utility; spell checker helper; spell check helper; spell checker helper tool; spell check helper tool; spell checker helper software; spell check helper software; spell checker helper service; spell check helper service; spell checker helper library; spell check helper library; spell checker helper sdk; spell check helper sdk; spell checker helper code; spell check helper code; spell checker helper script; spell check helper script; spell checker helper program; spell check helper program; spell checker helper application; spell check helper application; spell checker helper app; spell check helper app; spell checker helper tool; spell check helper tool; spell checker helper utility; spell check helper utility; spell checker helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
